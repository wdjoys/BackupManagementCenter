# 新部署：Server 直接提供 TLS
BMC_PUBLIC_URL=https://backup.example.com
BMC_TLS_CERT_FILE=./secrets/server.crt
BMC_TLS_KEY_FILE=./secrets/server.key
# 可选覆盖：
# BMC_SERVER_IMAGE=wdjoys/backup-management-center-server:latest
# VERSION=0.1.0
